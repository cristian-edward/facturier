{# va extinde product_layout.html.twig #}
{% extends 'base.html.twig' %}
{% form_theme form ':Form:filds.html.twig' %}

{% block submenu %}
    {# aducem linkurile din product_layout.html.twig #}
    {{ parent() }}
    {#  linkurile existente #}
    <h5> Feature name actions </h5>
    <ul class="nav nav-sidebar">
        <li>
            <a href="{{ path('product_new') }}">Add new</a>
        </li>
        <li>
            <a href="{{ path('product_index') }}">List</a>
        </li>
    </ul>
{% endblock %}
{% block body %}
    <h1>Product Edit</h1>
    {{ form_start(form, {'attr': {'role':'form', 'class': 'form-horizontal'}}) }}


        {{ form_label(form) }}


            {{ form_widget(form) }}

        {{ form_errors(form) }}


    {{ form_end(form) }}

    {#{{ form_start(delete_form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="form-group">

        <div class="col-md-3 col-md-offset-1 col-sm-4 col-sm-offset-2">
            {{ form_widget(delete_form.submit, { 'label': 'Delete', 'attr': {'class': 'btn btn-sm btn-danger'}}) }}
        </div>

    </div>
    {{ form_end(delete_form) }}#}
{% endblock %}
