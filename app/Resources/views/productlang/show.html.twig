{% extends 'base.html.twig' %}
{% form_theme delete_form ':Form:fields.html.twig' %}
{% trans_default_domain 'AppBundle' %}

{% block submenu %}
    {{  parent() }}
    <hr />
    <ul class="nav nav-sidebar">
        <li><a href="{{ path('productlang_new') }}">{% trans %}Add new{% endtrans %}</a></li>
        <li><a href="{{ path('productlang_index') }}">{% trans %}List{% endtrans %}</a></li>
        <li><a href="{{ path('productlang_edit', {'id':productLang.id}) }}">{% trans %}Edit{% endtrans %}</a></li>
    </ul>
{% endblock %}
{% block body %}
    <h1>ProductLang</h1>

<div class="col-lg-4">
    <div class="table-responsive">
        <table class="table table-bordered table-hover record_properties">
            <tbody>
                <tr>
                    <th>{% trans %}Id{% endtrans %}</th>
                    <td>{{ productLang.id }}</td>
                </tr>
                <tr>
                    <th>{% trans %}Name{% endtrans %}</th>
                    <td>{{ productLang.name }}</td>
                </tr>
                <tr>
                    <th>{% trans %}Lang{% endtrans %}</th>
                    <td>{{ productLang.lang }}</td>
                </tr>
            </tbody>
        </table>
    </div>

            {{ form_start(delete_form, {'attr': {'role':'form', 'class':'form-horizontal'}}) }}
                {{ form_widget(delete_form) }}
            {{ form_end(delete_form) }}
</div>
{% endblock %}
